/* La fonction BDDE pour Base de Données Empreinte prend en entrée une base de données de musiques et renvoie
 * la base de données empreinte correspondante. 
 */

object BDDE {

  // Certaines fonctions auxiliaires n'étant pas présentes, la fonction est mise en commentaire pour éviter
  // les erreurs.
  
/*  

  def BDDE(tab: Array[Array[Array[Int]]]): Array[Array[Array[Array[Double]]]] = {

    var bdde: Array[Array[Array[Array[Double]]]] = Array()
    var n: Int = tab.length
    var k: Int = 1
    for (i <- 0 to n - 1) {
      println("Musique " + k + " sur " + n)
      bdde = bdde ++ Array(empreinte(tab(i)))
      k = k + 1
    }
    return bdde
  } 

*/

}