/* La fonction fftm est la fonction qui regroupe toutes les étapes de l'algorithme FFT et
 * renvoie le tableau des fréquences.
 */

object fftm {

  // Certaines fonctions auxiliaires n'étant pas présentes, la fonction est mise en commentaire pour éviter
  // les erreurs.
  
/*

  def fftm(tab: Array[Array[Int]]): Array[Array[Double]] = {
    var n: Int = tab.length - 1
    var res: Array[Array[Double]] = Array(Array())
    res(0) = ItoD(tab(0))
    println("Veuillez patientez.. Etape 1 sur 2")
    for (i <- 1 to n) {
      res = res ++ Array(fftr(fft(ReToIm(tab(i)))))
    }
    println("Veuillez patientez.. Etape 2 sur 2")
    return res
  }
 
 
*/

}