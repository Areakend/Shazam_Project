/* La fonction module renvoie le module d'un complexe
 */

import scala.math.{ Pi, cos, sin, cosh, sinh, abs, sqrt }

object module {

  def module(C: complexes): Double = {
    var a = Seq(0)
    var b = sqrt(a(0) * (a(0)) + a(1) * (a(1)))
    return (b)
  }

}