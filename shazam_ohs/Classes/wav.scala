/* La fonction wav renvoie le tableau de tableaux d'entiers wav2D d'une musique à partir de son nom
 * Elle utilise les fonctions auxiliaires de l'API.
 */

object wav {

  // Certaines fonctions auxiliaires n'étant pas présentes, la fonction est mise en commentaire pour éviter
  // les erreurs.

/*  
 
  def wav(musique: String): Array[Array[Int]] = {
    
    var filePath: String = "C:/Users/Thibault/Documents/Ecole/shazam_ohs/Musique/Echantillon/" + musique;
    var wrappedWav: WavWrapper = new WavWrapper(filePath)
    var wav2D: Array[Array[Int]] = wrappedWav.getWav()
    return wav2D
    
  }

*/

}